import XCTest
@testable import WeatherInfoPackage

final class WeatherInfoPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WeatherInfoPackage().text, "Hello, World!")
    }
}
